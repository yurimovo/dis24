export type Employee = {
    employee_name: string,
    employee_position: string,
    department_id: number
};

export type EmployeeInList = {
    employee_id: number,
    employee_name: string,
    employee_position: string
}